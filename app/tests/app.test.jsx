var expect = require('expect');

it('should properly run tests', () => {
    expect(1).toBe(1);
})

describe('App', () => {
    it('sobviously', () => {
        expect(1).toBe(1);
    })
});

describe('App2', () => {
    it('sobviously', () => {
        expect(1).toBe(1);
    })
});
